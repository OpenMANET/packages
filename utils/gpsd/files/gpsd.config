config gpsd 'core'
        option enabled '1'
        option device '/dev/ttyAMA0'
        option port '2947'
        option listen_globally '0'
        # Set to 1 to prevent receiver probing/configuration during diagnosis.
        option readonly '0'
